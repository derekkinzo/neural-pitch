[toolchain]
# Pinned to 1.88: RUSTSEC-2026-0009 is fixed only in `time` 0.3.47,
# which requires rustc 1.88. Also covers edition 2024 + workspace.lints
# (available since 1.85).
channel = "1.88"
components = ["rustfmt", "clippy", "rust-analyzer", "rust-src", "rust-docs"]
profile = "default"
