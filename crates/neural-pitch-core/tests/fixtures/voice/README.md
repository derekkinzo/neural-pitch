# Synthetic voice fixtures

These FLAC files are 100% synthetic. They are generated by

```text
cargo run --example build_voice_fixtures --release
```

from `crates/neural-pitch-core/examples/build_voice_fixtures.rs`, which
calls `neural_pitch_core::test_utils::voice::synth_voice`. Synthesis is
closed-form deterministic — identical inputs always produce
byte-identical output — so the committed FLAC bytes here are
identical to what `cargo run --example build_voice_fixtures --release`
produces from a clean checkout.

## What they contain

A 4-partial harmonic stack with a glottal-source spectral tilt, optional
vibrato, and a three-formant cascade biquad band-pass roughly modelling
a neutral schwa vowel. Every fixture is 48 kHz / 24-bit / mono /
~1.5 s long. See `MANIFEST.toml` (in this directory) for the canonical
schema and per-fixture parameters.

## Licensing

These files contain **no third-party audio**. They are first-party
synthetic output and are covered by the workspace dual licence
(MIT OR Apache-2.0); see `LICENSE-MIT` and `LICENSE-APACHE` at the
repo root.

## Re-generating

Re-run the example from any clean working tree to regenerate. The
output is checked into git so that running the acceptance harness
(`scripts/run-acceptance.sh`) does not require an extra build step.
